const autenticado = false;

if(autenticado) {
    console.log('Si puedes ver Netflix')
} else {
    console.log('No, no puedes verlo')
}

// Operador ternario

console.log(autenticado ? 'Si esta autenticado' : 'No esta utenticado');