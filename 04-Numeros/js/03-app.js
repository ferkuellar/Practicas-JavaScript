let resultado;

// PI

resultado = Math.PI;

// si queremos redondear

resultado = Math.round(2.8);
resultado = Math.round(2.2);
resultado = Math.round(2.5);

// Redondeandar hacia arriba

resultado = Math.ceil(2.1);

// Redondeandar hacia abajo

resultado = Math.floor(2.9);

// Raiz cuadrada

resultado = Math.sqrt(144);

// Valor absoluto 

resultado = Math.abs(-500);


//Potencia

resultado = Math.pow(2, 8);

// Minimo en serie de numeros

resultado = Math.min(3, 5, -1, -10, 10);

// Maximo en serie de numeros

resultado = Math.max(3, 5, -1, -10, 10);

// Aleatorio pocas veces da numeros enteros

resultado = Math.random() * 20;

//Aleatoria dentro de un rango...

resultado = Math.floor(Math.random() * 30);


console.log(resultado);